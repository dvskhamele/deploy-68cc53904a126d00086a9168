#!/bin/bash
# Deployment script for TK999 Betting Platform

echo "Preparing TK999 Betting Platform for Netlify deployment..."

# Build the project
cd /Users/test/startups/deploy-68cc53904a126d00086a9168/tk999-deployment/frontend
npm install
npm run build

echo "Build completed successfully!"
echo "Files are located in: /Users/test/startups/deploy-68cc53904a126d00086a9168/tk999-deployment/frontend/dist"

echo ""
echo "To deploy to Netlify:"
echo "Option 1: Netlify CLI (if installed)"
echo "  netlify deploy --site <your-site-id> --auth <your-auth-token>"
echo ""
echo "Option 2: Manual deployment"
echo "  1. Go to https://app.netlify.com"
echo "  2. Click 'Add new site' > 'Deploy manually'"
echo "  3. Drag and drop the 'dist' folder from the path shown above"
echo ""
echo "Option 3: Git-based deployment"
echo "  1. Push this repository to GitHub/GitLab/Bitbucket"
echo "  2. Connect your repository in Netlify dashboard"
echo "  3. Netlify will use the netlify.toml config in the root directory"
echo ""
echo "The netlify.toml file in the project root has been configured with:"
echo "  - Build command: cd tk999-deployment/frontend && npm run build"
echo "  - Publish directory: tk999-deployment/frontend/dist"
echo "  - Base directory: ."
echo "  - Redirects for SPA routing"
echo "  - Asset caching headers"